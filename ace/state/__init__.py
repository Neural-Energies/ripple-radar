"""Module 1 — the latent macro state.

A panel of macro series, made stationary, read point-in-time, and reduced to a
small number of latent factors. Everything downstream — the regime engine, the
surprise engine, the conditional reaction engine — consumes this rather than
raw series.
"""
